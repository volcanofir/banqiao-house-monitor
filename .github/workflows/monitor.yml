name: 板橋指定路段房屋監控（舊版停用）

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy monitor disabled
        run: echo "Legacy scheduled monitor is disabled. Use volcanofir/banqiao-house-monitor-runner instead."
